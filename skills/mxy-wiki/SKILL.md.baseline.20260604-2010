---
name: mxy-wiki
version: 2.3.0
author: mxyinvoker
description: |
  通用的元数据驱动个人知识库构建系统。基于 Karpathy llm-wiki 方法论，融合
  元数据替代 index.md、三阶段 lint、反幻觉防御等改进。
  通过对话指定目录并加载其中的 .wiki-schema.md 来适配任意知识库。
metadata:
  hermes:
    tags: [wiki, obsidian,knowledge-base, notes, metadata-driven, hallucination-defense]
    category: note-taking
    related_skills: [obsidian-base, obsidian-markdown, obsidian-cli, defuddle]
---

# Meta Wiki — 通用元数据驱动知识库

基于 [Andrej Karpathy 的 LLM Wiki 理论](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) 和
[sdyckjq-lab/llm-wiki-skill](https://github.com/sdyckjq-lab/llm-wiki-skill) 的工程实践，
重新设计的通用知识库构建系统。**不绑定特定 vault**，通过 `.wiki-schema.md` 适配任意知识库。

## Vault 路径发现

LLM 按以下优先级确定操作目标：

```
1. 用户明确指定路径
2. 当前工作目录包含 .wiki-schema.md → 使用当前目录
3. 两者都没有 → 询问用户：「要操作哪个知识库目录？」
```

一旦确定路径，后续所有操作都基于该路径。

---

## .wiki-schema.md 规范

每个知识库根目录下必须有一个 `.wiki-schema.md`。
**模板文件**：`templates/schema-template.md`，包含完整的 frontmatter 配置、注释、Tag 惯例和目录结构说明。新建 vault 时直接拷贝此模板并填入 vault 特定配置。

**评审记录**：`references/review-notes.md` — 上次端到端评审的修复摘要和剩余建议。

### 必需字段

```markdown
---
language: zh                 # zh | en
description: "..."           # 自然语言描述 vault 用途，LLM 据此理解知识库类型
---
```

**模板文件**：`templates/schema-template.md`，包含完整可配置项和注释。

---

## Vault 目录结构

```
vault根/
├── .wiki-schema.md             # LLM 操作手册
├── raw/                        # 原始素材（不可变）
│   └── YYYY/MM/DD/
│       └── filename.md
├── wiki/                       # LLM 维护的知识页面
│   ├── go/                     # 领域目录（一级，不嵌套）
│   ├── mysql/
│   ├── 分布式/
│   ├── 历史/
│   ├── 经济/
│   └── _synthesis/             # 跨领域综合暂存区
└── 跨领域综合.md               # 不归属单一领域的 synthesis 页
```

**目录规则**：
- 领域目录只保留一级，不嵌套（`wiki/go/` ✅，`wiki/go/performance/` ❌）
- 分类信息在 frontmatter 的 `type` 和 `tags` 字段，不在路径里
- LLM 用 `search_files` 按 tag 搜索，不依赖目录结构
- `_synthesis/` 是临时寄存区，积累到阈值后可能升级为独立领域目录

---

## 文章放置决策

新文章 ingest 时，LLM 按以下决策树选择 wiki 页面的放置位置：

### 决策流程

```
文章提炼出的 entities → 判断领域归属 →

  ① 内容可拆分为多个独立领域 → Hub-and-Spoke（方法 3）
     拆分后每部分独立可读 → 放入各领域目录
     同时在主导领域创建 hub 页
     hub 页包含文章概览 + [[拆分后各页面]] 的 wikilinks

  ② 不可拆分（拆分后页面残缺）→ _synthesis/（方法 2）
     放入 wiki/_synthesis/ 暂存
     等待同 tag 文章积累到阈值后独立

  ③ 无法判断 → 默认走 ②，等下次 lint 阶段 C 再评估
```

### Hub-and-Spoke 模式（方法 3）

适合：文章涉及多个领域，但每个部分独立可读。
拆分后每个领域页面的正文 ≥ 300 字。不达标的走 `_synthesis/` 暂存。

**Hub 页模板**：

```markdown
---
title: "分布式事务实践（总览）"
created: 2026-05-13
updated: 2026-05-13
status: stable
type: synthesis
tags: [分布式/事务, go/并发]
source: "raw/2026/05/13/分布式事务实践.md"
confidence: EXTRACTED
---

# 分布式事务实践（总览）

> 源文摘要：本文探讨了在 Go 微服务中实现分布式事务的几种方案，包括两阶段提交、
> Saga 模式和 TCC 模式，并给出了性能对比。

## 拆分页面

- [[Go 事务与并发控制]] — Go 侧的事务机制和并发安全
- [[分布式事务与两阶段提交]] — 分布式理论侧的两阶段提交协议

## 源文关键数据

| 方案 | 吞吐量 | 延迟 | 复杂度 |
|---|---|---|---|
| 两阶段提交 | 500 TPS | 200ms | 低 |
| Saga | 2000 TPS | 50ms | 中 |
| TCC | 1500 TPS | 80ms | 高 |
```

**规则**：hub 页 ≤ 50 行。只包含源文摘要、拆分页面 wikilinks、关键数据表格。不重抄源文。

### _synthesis/ 暂存（方法 2）

适合：文章不可拆分，或领域尚未成熟。

```
wiki/_synthesis/
├── 分布式锁的工程实践.md       # 涉及 Go + Redis + 分布式，不便拆分
├── 区块链与经济货币.md         # 区块链 + 经济，领域尚未独立
└── 宋代科技与社会变迁.md       # 历史 + 科技，待积累
```

**升级规则**：lint 阶段 B 扫描 `_synthesis/`，当同 tag 文章 ≥5 篇时 → 建议升级为独立领域目录。

---

## MOC（Maps of Content）策略

MOC 是导航笔记，通过 `[[wikilinks]]` 引导进入某个主题的知识网络。由 LLM 自动维护。

### 领域 MOC（Dataview 动态生成）

领域内页面较多（10+）且持续增长时使用。新笔记自动出现，零维护成本。

```markdown
# Go 性能优化 MOC

```dataview
TABLE status, confidence, updated
FROM "wiki/go"
WHERE contains(tags, "performance") OR contains(tags, "optimization")
SORT updated DESC
```
```

### 跨领域 MOC（静态 Markdown，LLM 维护）

跨多个领域（go + mysql + redis + ...）的导航页。LLM 在 lint 阶段 C 或 ingest 后更新。

```markdown
# 后端性能全景 MOC

> 最后更新：2026-05-13 | 涵盖 Go / MySQL / Redis / 分布式

## Go
- [[Go GC 调优]] — 三色标记、GOGC 调参
- [[Go 网络编程性能优化实践]] — 连接池、bufio、零拷贝

## MySQL
- [[MySQL 索引优化]] — B+树、覆盖索引、索引下推
- [[InnoDB 锁机制]] — 间隙锁、MVCC

## Redis
- [[Redis 缓存策略]] — 淘汰算法、缓存穿透、缓存雪崩

## 跨领域
- [[后端性能优化方法论]] — 从 profiling 到生产部署
```

**维护触发条件**：
- 领域内笔记超过 5 篇 → 创建领域 MOC
- 跨领域 MOC 引用的页面有新增/删除 → lint 阶段 C 更新
- 用户说「更新 MOC」→ 立即更新

### Hub 页（文章级 MOC）

每次 Hub-and-Spoke 拆分时自动创建。本质上是单篇文章的 MOC。

**对比三种 MOC**：

| | 领域 MOC | 跨领域 MOC | Hub 页 |
|---|---|---|---|
| 面向对象 | 单一领域 | 多个领域 | 单篇文章 |
| 实现 | Dataview 动态 | 静态 Markdown | 静态 Markdown |
| 维护者 | 自动（Dataview 查询） | LLM（lint 阶段 C） | LLM（ingest 时一次创建） |
| type | concept | synthesis | synthesis |
| 示例 | `Go 性能优化 MOC` | `后端性能全景 MOC` | `分布式事务实践（总览）` |

---

## Frontmatter 规范

所有知识库页面必须使用 Obsidian 兼容的 YAML frontmatter。
关于 Obsidian 的 wikilinks 语法、callouts、embeds、properties 等，**加载 obsidian-markdown skill 获取完整参考**。

### 核心字段

```yaml
---
title: "页面标题"
created: 2026-05-13
updated: 2026-05-13
status: stable          # draft | stable | needs-review | outdated | superseded
type: concept           # concept | source-summary | comparison | question | synthesis
tags: [go/gc, go/memory]  # 见 Tag 惯例
source: ""              # 源材料 URL（ingest 时必填）
confidence: EXTRACTED   # EXTRACTED | INFERRED | AMBIGUOUS | UNVERIFIED
contested: false
contradictions: []
related: []
---
```

### 字段说明

| 字段 | 必填 | 说明 |
|---|---|---|
| `title` | 是 | 页面标题，支持中文 |
| `created` | 是 | ISO 日期 YYYY-MM-DD |
| `updated` | 是 | 每次修改必须更新 |
| `status` | 是 | draft → stable → needs-review → outdated → superseded |
| `type` | 是 | concept（知识点）/ source-summary（素材摘要）/ comparison（对比）/ question（存回的查询）/ synthesis（综合） |
| `tags` | 是 | YAML list 格式，见下方惯例 |
| `source` | ingest 时必填 | 原始素材 URL 或文件路径 |
| `confidence` | ingest 时必填 | 见置信度体系 |
| `contested` | 矛盾时 | true 表示存在未解决的矛盾 |
| `contradictions` | contested=true 时 | `[[矛盾页面]]` 列表 |
| `related` | 建议 | `[[双向链接]]` 列表 |

### Obsidian 双向链接

页面之间的引用使用 Obsidian wikilink 格式。详见 obsidian-markdown skill。

```markdown
[[Go GC 调优]]               # 标准链接
[[Go GC 调优|GC 优化]]        # 带显示别名
[[Go GC 调优#三色标记]]       # 链接到子标题
```

**页面名 = 文件名 stem（不含路径和 .md 扩展名）**。文件 `wiki/go/Go GC 调优.md` 的页面名是 `Go GC 调优`。wikilinks 中使用的目标必须与 stem 一致。文件命名时确保 stem 能作为 wikilink 目标。

在 frontmatter 的 `related` 和 `contradictions` 字段中**只写页面名**：

```yaml
related:
  - "Go GC 调优"
  - "零拷贝与 sendfile"
contradictions:
  - "InnoDB 锁机制"
```

在页面正文中才使用完整 `[[wikilink]]` 语法。

---

## Tag 惯例

不使用预定义清单。LLM 遵守以下规则：

1. 全小写、单数、连字符分隔。✅ `connection-pool` ❌ `ConnectionPool`
2. 每页至少 1 个 tag，数量不限，宁少勿多
3. 使用 `领域/子类` 嵌套格式：✅ `go/gc` `mysql/index` `历史/宋朝`
4. 已有同义 tag 直接复用。✅ 已有 `kubernetes` 用它 ❌ 另写 `k8s`
5. 技术名词用最常用写法（`postgresql` 不用 `pg`；`javascript` 不用 `js`）
6. 元信息（状态、优先级、日期）放 frontmatter 字段，**绝不放 tag**
7. 全新领域只用父 tag（如 `wasm`），不加子类；积累够再细分

**Lint 自动检测**：同名异写、单次出现的 tag、含义重复的 tag、跨领域子 tag 相同但未关联的页面。

### 跨领域交叉引用

同一概念在不同领域的笔记共享子 tag（如 `go/gc` 和 `计算机基础/gc`），
lint 的 `similar_unlinked` 会通过子 tag 匹配发现它们。
LLM 应在 ingest 时主动添加跨领域 `[[wikilinks]]`。例如，发现 `go/gc` 和 `计算机基础/gc` 共享子 tag `gc` 时，在两页面互相添加 `related` 引用：

```yaml
# 在 wiki/go/Go GC 调优.md 中
related:
  - "垃圾回收算法"
```

---

## 置信度体系

每条写入知识库的 claim 必须在 frontmatter 标 `confidence`。

| 级别 | 含义 | 判断标准 |
|---|---|---|
| `EXTRACTED` | 源文直接陈述 | 能在源文中找到字面对应的句子 |
| `INFERRED` | 从源文内容推理 | 推理链每一步都能在源文找到锚点 |
| `AMBIGUOUS` | 源文表述含糊或有歧义 | 源文本身就有多种理解方式 |
| `UNVERIFIED` | 推理链中有一步纯靠 LLM 背景知识 | 源文中找不到对应锚点 |

**INFERRED vs UNVERIFIED 的判定口诀**：推理结论如果要写成「根据源文第 X 段可知……」→ INFERRED；如果写成「据我所知……」→ UNVERIFIED。

**硬性锚点规则**：如果一条 claim 无法在源文中定位到具体句子作为锚点，即使推理看起来「合理」，也必须降级为 UNVERIFIED。LLM 在分析时必须为每个 INFERRED claim 指明锚点在源文中的位置（如段落号或关键句）。找不到锚点的 claim 自动归类为 UNVERIFIED。

**页面级 vs 段落级**：一个页面可能包含不同置信度的内容。处理策略：

- frontmatter 的 `confidence` 标注该页面**最高**置信度（代表最可靠的部分）
- 正文中较低置信度的段落用 Obsidian callout 单独标注：
```markdown
> [!warning] INFERRED — 以下为推理结论
> 从 GC 暂停时间数据推断，该优化对大堆效果更显著。
```
- lint 阶段 C 会收集所有低于 `EXTRACTED` 的 claim 并建议改善

---

## 会话初始化

每次与新 vault 交互或用户开始新会话时，LLM 按以下步骤建立上下文。

**Step 1 — 发现 vault 路径**（按优先级）：
1. 用户明确指定路径 → 使用
2. 当前工作目录包含 `.wiki-schema.md` → 使用当前目录
3. 都没有 → 询问用户

**Step 2 — 读取 schema**：
```bash
read_file <vault_path>/.wiki-schema.md
```
确定语言、过期阈值、排除目录等。

**Step 3 — 按需获取上下文**（替代 log.md）：
**不维护全局日志文件**。跨会话上下文通过 obsidian-cli 获取：

```bash
# 获取最近修改的页面（了解最近知识库变化）
obsidian vault="<vault>" search query="updated: 2026"

# 获取特定领域的页面（用户提到 Go 时）
obsidian vault="<vault>" search query="tag:go"

# 获取状态异常的页面
obsidian vault="<vault>" search query="status: needs-review OR status: outdated"
```

如果 Obsidian 未运行，降级为 `search_files` regex 匹配 frontmatter。

**不需要知道「之前做了什么操作」**，只需要知道「现在有哪些页面、它们的状态是什么」。
frontmatter 的 `updated`、`status`、`tags`、`source` 字段提供了足够的信息。

---

## 操作

### 1. Ingest（消化素材）

**注意**：ingest 分为两个阶段，中间可断点续做。

**阶段 1：分析**（步骤 ①-④）— 保存 raw、隐私检查、搜索已有页面、输出 JSON。
**阶段 2：生成**（步骤 ⑤-⑧）— 页面决策、写入、回写 extracted_to、inline lint、输出变更清单。

阶段 1 完成后，LLM 展示 JSON 摘要让用户确认（特别是 `suggested_tags` 和 `entities` 概览），确认后再进入阶段 2。

**⓪ Step 0 — 搜索已有页面**（在结构化分析之前强制执行）

```bash
obsidian vault="<vault>" search query="<主题关键词>"
obsidian vault="<vault>" search query="tag:<相关领域>"
```

读取搜索结果中 top 5 相关页面的 frontmatter + 首段。
这一步的唯一目的是让 LLM 了解 vault 中已有哪些相关知识，避免写出与已有页面完全重复的内容。

**不做的事**：不在此阶段检测矛盾、不判断合并、不生成 `new_vs_existing`。
矛盾检测与合并全部留给 lint 阶段 B/C。

如果 Obsidian 未运行，降级为 `search_files`。

**⓪.5 选择素材获取工具**（素材来源为 URL 时必须执行）

```
素材来源是 URL？
  ├─ 静态文档/博客 → defuddle（先加载 defuddle skill）
  │   └─ defuddle 失败 → curl -k 下载 + defuddle 解析本地文件
  │       └─ curl 也失败 → web_extract
  ├─ API 端点/简单页面 → web_extract
  └─ 动态页面/需要交互 → browser_navigate
```

**① 保存原始素材**

> **⚠️ raw/ 不可变**：保存后 raw/ 文件正文**永远不能被替换或修改**，即使后来用更好的工具（如 defuddle）重新提取了更干净的内容。
> 唯一允许修改的字段是 `extracted_to`。如果重新提取的结果更好 → 直接在 wiki/ 页面中改善，或以新文件名存入 raw/。**绝不覆盖已有 raw/ 文件正文**。

路径格式：`raw/YYYY/MM/DD/filename.md`

```
raw/
├── 2026/
│   ├── 05/
│   │   ├── 13/
│   │   │   ├── goperf-networking.md
│   │   │   └── another-article.md
│   │   └── 12/
│   └── 04/
```

文件名保持语义（文章标题），不重复日期。raw/ 文件包含自己的 frontmatter：

```yaml
---
source_url: https://goperf.dev/02-networking/
ingested: 2026-05-13
extracted_to:            # 记录知识流向（反向链接）
  - "Go 网络编程性能优化实践"
  - "Go GC 调优"
  - "零拷贝"
---
```

**② 隐私自查** — 提醒用户检查手机号、身份证、API 密钥、明文密码。不自动过滤。

**③ Step 1 — 结构化分析**

输出 JSON（不持久化到 vault）。完整 schema：

```json
{
  "source_summary": "一句话概括文章内容",
  "entities": [
    {
      "name": "Go GC 三色标记",
      "type": "concept",
      "relevance": "high",
      "confidence": "EXTRACTED",
      "evidence": "原文：Go 1.5 开始使用并发三色标记清除算法"
    }
  ],
  "topics": [
    {"name": "Go 性能优化", "importance": "high"}
  ],
  "connections": [
    {
      "from": "Go GC 三色标记",
      "to": "写屏障",
      "type": "因果",
      "confidence": "INFERRED",
      "evidence": "三色标记需要写屏障保证并发安全，源文描述了写屏障触发条件"
    }
  ],
  "contradictions": [],
  "suggested_tags": ["go/gc", "go/memory"]
}
```

**字段约束**：
- `entities[].confidence`：必须是 `EXTRACTED|INFERRED|AMBIGUOUS|UNVERIFIED`
- `connections[].confidence`：同上
- `EXTRACTED`/`INFERRED` 必须提供 `evidence`（≤50 字）
- `suggested_tags` 使用 `领域/子类` 嵌套格式

**重要**：此阶段不做合并决策。每篇文章产生自己的页面，
页面是否需要合并由 lint 阶段 B/C 的 `similar_unlinked` 和 tag 聚类统一处理。

**④ 用户确认** — 展示 JSON 摘要（entities 数量、suggested_tags、发现的 contradictions），确认后进入阶段 2。

**⑤ Step 2 — 页面生成**

根据「文章放置决策」选择策略：

- **可拆分 → Hub-and-Spoke**：拆分为独立页面放入各领域目录，在主导领域创建 hub 页（`type: synthesis`）
- **不可拆分 → _synthesis/**：整篇放入 `wiki/_synthesis/`
- **无法判断 → 默认 _synthesis/**

**不在此阶段做合并或更新**。每篇文章产生独立的页面。
页面是否需要合并、交叉引用、追加到已有页面——全部由 lint 阶段 B 的 `similar_unlinked` 检测和阶段 C 的语义判断统一处理。

所有页面遵循 frontmatter 规范：
- `source` 字段指向 raw/ 文件路径
- `confidence` 标注页面的最高置信度
- EXTRACTED 级别必须在正文中用 `> 摘录：...` 引用原文（≤50 字）

**EXTRACTED 摘录写作流程**（必须按此顺序）：

1. 在 raw/ 源文中找到支撑 claim 的**原文句子**
2. **复制精确子串**（不改写、不缩写、不合并）
3. 粘贴为 `> 摘录：原文子串`（≤50 字则完整保留，>50 字则截断保留前 50 字原文前缀）
4. **然后**在摘录块下方写你的分析/总结

❌ 错误：先写分析，再补摘录（容易变成改写）
❌ 错误：`> 摘录：该文介绍了 Go GC 的三色标记算法`（这是你的概括，不是原文）
✅ 正确：`> 摘录：Go 1.5 开始使用并发三色标记清除算法`（原文子串）

**⑥ 更新 raw/ 的 extracted_to**

页面生成完成后，回写 raw/ 文件的 `extracted_to` 字段，记录这篇素材的知识流向了哪些 wiki 页面。这是从 raw → wiki 的反向索引。

**⑦ Inline Lint**（每次自动做） — 检查 wikilinks 有效性、tag 是否违反惯例。

**⑧ 输出变更清单**

ingest 完成后，LLM 输出本次创建/修改的所有文件路径列表：

```
本次 ingest 创建：
  raw/2026/05/13/分布式事务实践.md
  wiki/go/Go 事务与并发控制.md
  wiki/分布式/分布式事务实践（总览）.md

本次 ingest 修改：
  raw/2026/05/13/分布式事务实践.md（extracted_to）
```

用户说「撤销」→ LLM 删除这些新创建的文件，还原 raw/ 的 `extracted_to` 为 ingest 前的状态。

**Ingest 后 lint**：ingest 完成后，如果用户说「lint 一下」，优先执行**定向 lint**（只检查本次 ingest 涉及的领域），而非全局 lint。流程见下方「定向 lint」。

### 2. Query（查询）

**① 元数据定位**（不依赖 index.md、不依赖 log.md）：

```bash
# 首选：obsidian-cli 按 tag 搜（原生理解 property 系统）
obsidian vault="<vault>" search query="tag:<key>"

# 补充：search_files 按正文关键词搜索（不限于 frontmatter）
search_files pattern="<keyword>" target="content" path=<vault_path> file_glob="*.md"
```

排除范围：`raw/` 目录不参与查询，`.wiki-schema.md` 不参与查询。
**注意**：`search_files` 不支持按目录排除。LLM 必须在结果中**手动过滤**掉路径包含 `raw/` 或文件名为 `.wiki-schema.md` 的条目。

**② 读相关页面** — 优先读 frontmatter 判断相关性（`type`、`tags`、`confidence`）。

**③ 综合回答** — 标注来源页面 + 置信度。每个事实性声明后必须跟置信度标签：

```
格式：[EXTRACTED: 来源页面名] 或 [INFERRED] 或 [UNVERIFIED]

例：
Go 1.5 开始使用并发三色标记清除算法 [EXTRACTED: Go GC 调优]。
在极端负载下 STW 时间可能高达 10ms [INFERRED]。
```

**强制规则**：
- 每个独立的事实性声明必须单独标注置信度
- 长回答末尾附置信度汇总表
- 不得在回答末尾一次性标注「以上信息均为……」— 这是偷懒
- `status: outdated` 或 `superseded` 的页面，回答中必须显式告知「该信息可能已过时」

**④ 可选持久化** — 用户确认后保存为 wiki 页面。保存时 type 设为 `question`，confidence 与回答中的标注一致。

### 3. Lint（健康检查）

**阶段 A — Ingest inline**（每次 ingest 自动执行）：
- Wikilinks 有效性（`[[X]]` 指向的文件是否存在）
- Tag 是否违反惯例
- 新产生的 contradictions 是否正确标记

**阶段 B — 全局机械扫描**（用户说「lint」时执行）：

1. **确认 scope** — 告知用户本次 lint 的范围（全局 or 定向），列出将检查的文件数量/领域，**询问用户确认后**再执行脚本
2. 执行脚本：
```bash
python3 <skill_dir>/scripts/lint-stage-b.py <vault_path>
```
3. 报告以 JSON 输出到 stdout，LLM 读取后进入阶段 C。

**检查范围说明**：

| 检查 | 全局/局部 | 原因 |
|---|---|---|
| 孤页 | 全局 | 需要完整的入链图才能判断零入链 |
| 断链 | 全局 | 需要知道所有页面 stem 才能判断目标是否存在 |
| Tag 相似未关联 | 全局 | 需要所有页面的 tag 集合做交叉比较 |
| Tag 审计 | 全局 | 需要全量 tag 统计 |
| Frontmatter 完整性 | 局部 | 只检查该页面自身字段 |
| EXTRACTED 摘录真实性 | 局部 | 只需该页面 + 对应 raw/ 文件 |
| _synthesis/ 升级 | 全局 | 需要 _synthesis/ 下所有文件的 tag 聚类 |

定向 lint 时：脚本全量跑（数据收集），结果过滤展示（只看 scope 内）。

1. **孤页检测** — 所有 `[[wikilinks]]` 构建入链图，标记零入链页面
2. **断链检测** — `[[X]]` 但 X.md 不存在于 vault 中
3. **Tag 相似但未关联** — tag 重叠 ≥ `tag_overlap_min` 但没有互链的页面对
4. **Tag 审计** — 同名异写（go/Go/golang）、单次出现的 tag（疑似拼写错误）、含义重复（kubernetes/k8s）
5. **Frontmatter 完整性** — 必填字段缺失、updated 早于 created 等异常
6. **EXTRACTED 摘录真实性** — 对 `confidence: EXTRACTED` 且 `source` 指向 raw/ 的页面，验证摘录在源文中能否找到（子串匹配）
7. **_synthesis/ 升级检测** — 扫描 `_synthesis/` 下文件，按父 tag 前缀聚类。同 tag 文件 ≥ `synthesis_threshold` 篇 → 建议升级为独立领域目录

报告以 JSON 输出，LLM 读报告后**先展示汇总**（orphans 数量、broken_links 数量等），然后**询问用户**是否执行阶段 C。除非用户明确说「deep lint」或「继续 C」或「全部修复」，否则不自动进入阶段 C。

**定向 lint**（只检查本次 ingest 涉及的知识领域）：

用户说「lint 缓存相关的页面」而非全局 lint 时，执行以下流程：

```
① 确定 scope
   从新页面的 tags 提取领域关键词（如 redis/缓存一致性 → redis/）
   搜索 vault 中同 tag 的页面 + 新页面 related/[[wikilinks]] 引用的页面
   列出候选文件清单，询问用户：追加 / 去掉 / 确认

② Stage B 全量跑（脚本不改，几秒完成不费 token）
   python3 <skill_dir>/scripts/lint-stage-b.py <vault_path>

③ 过滤 JSON 结果到 scope
   只保留 path 在 scope 文件列表中，或 similar_unlinked 涉及 scope 内页面的条目
   展示给用户

④ Stage C 语义检查（只读 scope 内文件）
   矛盾检测 + 合并建议 + EXTRACTED 摘录回溯
```

**关键**：`similar_unlinked` 和 `orphan` 检查依赖全量索引，不能只扫 scope 文件。
所以脚本全量跑，结果过滤展示——数据收集全量，结果展示定向。

**阶段 C — LLM 语义检查**（仅在用户明确指令「deep lint」或同意执行时触发）：

依次执行以下操作：

1. **矛盾审查** — 对每个 `contested: true` 的页面：
   - `read_file` 读取该页面及其 `contradictions` 列表中的页面
   - 判断矛盾是否仍然存在（是否已有新信息解决）
   - 如可解决：更新双方页面，移除 `contested`/`contradictions`
   - 如不可解决：保留标记，加入报告「Y 个未解决的矛盾」

2. **可合并检测** — 对 `similar_unlinked` 中的前 10 对：
   - `read_file` 读取两页的 frontmatter + 首段
   - 判断是否内容重复度 > 50% → 建议合并
   - 判断是否需要添加交叉引用但不必合并 → 建议添加 `[[wikilinks]]`
   - 输出决策 + 理由

3. **低置信度审计** — 扫描 `confidence: UNVERIFIED` 或 `AMBIGUOUS` 的页面：
   - 列出所有此类页面，按 confidence 分组
   - 如果页面 `source` 存在且可回溯到 raw/ → 建议重新 ingest 升级置信度
   - 如果无 source → 建议查找原始资料或降级 status 为 `needs-review`

4. **EXTRACTED 证据回溯** — 对阶段 B 报告的 `confidence=EXTRACTED 缺少摘录` 页面：
   - `read_file` 该页面和对应的 raw/ 源文件
   - 在 raw/ 中搜索页面中 EXTRACTED 级别的 claim
   - 找到原文 → 补充 `> 摘录：...` 块
   - 找不到 → 降级为 `INFERRED` 并记录

阶段 C 完成后，以分级报告输出（阻断/需要关注/可忽略）。

### 4. 可选操作

- **批量消化** — 用户提供文件夹路径 → 逐个执行 ingest，每 5 个文件暂停询问是否继续
- **删除** — 删 raw/ 源文件 + 对应 wiki 页面 + 清理其他页面的 `related`/`contradictions` 引用 + 更新受影响 raw/ 文件的 `extracted_to`
- **结晶化** — 对话内容记入知识库，默认 confidence: INFERRED

---

## 反幻觉防御

### LLM 行为约束

**绝对不能做**：
- 把 INFERRED 标注为 EXTRACTED
- 编造不存在的原文摘录
- 强行调和矛盾观点
- 对不同来源的 INFERRED 内容做交叉验证后当成确定的结论

**应该主动做**：
- 推理链不完整 → 标注 AMBIGUOUS
- 背景知识补充 → 标注 UNVERIFIED
- 单源且无直接证据 → 默认 INFERRED

### 防御矩阵

| 防线 | 阶段 | 机制 |
|---|---|---|
| 置信度强制标注 | ingest | 每条 claim 必须标 EXTRACTED/INFERRED/AMBIGUOUS/UNVERIFIED |
| EXTRACTED 必须附原文 | ingest + lint stage B | 正文需含 `> 摘录：...`（≤50 字），脚本自动检查 |
| 矛盾即标记 | ingest | contested: true，保留双方观点 |
| 查询时显式告知 | query | INFERRED/AMBIGUOUS 信息标注置信度 |
| raw/ 不可变 | 全流程 | 永远可回溯原始资料 |
| 事实审计 | lint stage B + C | 脚本检查 EXTRACTED 缺摘录，LLM 回溯验证 |
| 不强行建立关联 | ingest | connections 默认不超过 INFERRED |

---

## 工具集成

### 元数据搜索（首选 obsidian-cli）

**所有按 frontmatter 字段（tags / status / type / updated）进行的搜索，
优先使用 obsidian-cli。** obsidian-cli 原生理解 Obsidian 的 property 系统，
比裸 `search_files` regex 匹配更可靠。

```bash
# 按 tag 查找页面
obsidian vault="<vault>" search query="tag:go"

# 获取所有 tag 的统计（发现同名异写、单次出现的 tag）
obsidian vault="<vault>" tags sort=count counts

# 查找特定文件的入链（孤页检测核心）
obsidian vault="<vault>" backlinks file="Go GC 调优"
```

**降级策略**：**必须先实际执行 obsidian-cli 命令验证可用性**，不能凭假设跳过。如果命令报错（找不到 obsidian、连接超时等），再降级为 `search_files`：

```bash
# 降级：用 search_files regex 匹配 frontmatter
search_files pattern="tags:.*go" target="content" path=<vault_path> file_glob="*.md"
```

### Obsidian 笔记操作

当需要创建/编辑 Obsidian 笔记、使用 wikilinks/callouts/embeds/properties 时，
**加载 `obsidian-markdown` skill** 获取完整语法参考。

当需要在 vault 中读写文件时，**加载 `obsidian` skill** 获取文件操作最佳实践。

### 搜索正文内容

搜索正文关键词时使用 `search_files`（content 模式），不依赖 obsidian-cli。

### 网页素材提取（ingest Step 0 前）

ingest 的素材来源是 URL 时，**先加载相关 skill 评估再选择工具**：

| 工具 | 适用场景 | 说明 |
|---|---|---|
| `defuddle` skill | 静态技术文档、博客文章 | 专用网页→干净 markdown 转换，结构保留更好 |
| `web_extract` | 简单页面、API 端点 | 通用提取，速度快但可能丢失复杂结构 |
| `browser_navigate` | 动态页面、需要交互 | 需要点击/滚动/JS 渲染的页面 |

不要惯性使用 `web_extract` 后就认为「够用了」——先评估替代方案。
详细对比和 defuddle fetch 失败的降级方案见 `references/web-extraction-comparison.md`。

---

## 常见错误（Pitfalls）

| 错误 | 触发场景 | 正确做法 |
|---|---|---|
| 覆盖 raw/ 文件正文 | 用更好的工具重新提取后想「升级」raw/ | 新建 raw/ 文件或直接改善 wiki/ 页面 |
| 写改写摘要为摘录 | 觉得原文太长，用自己的话缩写 | 必须从 raw/ 复制精确子串，≤50 字截断保留原文前缀 |
| 不验证就降级工具 | 惯性认为 obsidian-cli 不可用 | 先执行命令，报错才有降级依据 |
| 跳过用户确认直接 lint | 认为 lint 是无害操作 | Stage B 前确认 scope，Stage C 前确认执行 |

## 注意事项

1. **不修改 raw/ 下的任何文件（extracted_to 除外）** — 原始素材正文不可变。`extracted_to` 是唯一的例外，用于记录知识流向。
2. **不创建 index.md、不维护 log.md** — 用 frontmatter + search_files 替代所有索引和日志功能。
3. **元信息不放 tag** — status/draft/priority 用 frontmatter 字段。
4. **尊重现有目录结构** — 不强行套用子目录。
5. **不在 ingest 时做全局 lint** — 成本高且与本次无关。
6. **不确定就用 INFERRED** — 宁可保守，不过度承诺准确性。
7. **raw/ 按 Y/M/D 组织** — `raw/YYYY/MM/DD/filename.md`，与 Obsidian 日记结构一致。
8. **验证工具可用性再降级** — obsidian-cli 等工具的降级决策必须基于实际执行结果，不能凭假设跳过。先执行命令，报错再降级。所有「不可用」的判断都需要证据。
9. **选工具前加载相关 skill** — 有多个工具可选时（如 `web_extract` vs `defuddle` skill vs `browser_navigate`），先加载相关 skill 了解其能力和适用场景，再做选择。不要惯性使用默认工具后就认为「够用了」。
10. **EXTRACTED 摘录必须是源文子串** — `> 摘录：...` 的内容必须是 raw 源文的**精确子串**（脚本用子串匹配验证）。改写、缩写、合并多句都会导致 lint 失败。如果源文太长需要截断，保留前 50 字符的原文前缀。
11. **摘录中的特殊字符** — raw/ 文件可能使用弯引号（""''）、全角符号等 Unicode 字符。写摘录时**保持原文字符不变**，不要手动替换引号样式。lint 脚本会做 Unicode 归一化匹配。
